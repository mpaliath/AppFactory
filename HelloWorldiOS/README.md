# HelloWorldiOS

A minimal SwiftUI iOS app that displays **Hello, world!**.

## Open in Xcode

1. Open `HelloWorldiOS.xcodeproj` in Xcode.
2. Select an iOS Simulator (e.g., iPhone 15).
3. Build and run.
